To run the application, change the working directory to the project root directory and enter the command:

```
docker compose up
```

the backend runs on port `3000` while the frontend runs on port `3001`.
